name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Portfolio Check
        run: |
          echo "Testing portfolio structure..."
          ls -la
          grep "Welcome" index.html